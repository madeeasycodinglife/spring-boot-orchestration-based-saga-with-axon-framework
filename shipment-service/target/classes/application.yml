server:
  port: 8082
spring:
  application:
    name: SHIPMENT-SERVICE
  datasource:
    url: jdbc:h2:~/shipment-service
    driver-class-name: org.h2.Driver
    username:
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop


  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

